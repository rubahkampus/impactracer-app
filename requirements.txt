# AST Parsing (Offline Indexer)
tree-sitter>=0.22
tree-sitter-languages>=1.10.2
mistune>=3.0

# Embedding and Reranking (Local Inference)
# FlagEmbedding 1.4 requires torch>=2.6 (via transformers 4.53+ CVE-2025-32434 fix).
# Use torch 2.6.0+cu124 for GPU (RTX 3050): pip install torch==2.6.0+cu124 --index-url https://download.pytorch.org/whl/cu124
# Pin the full stack to avoid ABI mismatches between pyarrow, tokenizers, and transformers.
FlagEmbedding>=1.4.0
transformers>=4.53.0,<5.0
tokenizers>=0.22.0,<=0.23.0
pyarrow>=21.0.0
huggingface-hub>=0.34.0,<1.0

# LLM Client (OpenRouter via httpx — exclusive provider)
httpx>=0.27

# Vector and Relational Stores
chromadb>=0.5.0
rank_bm25>=0.2.2

# In-Memory Graph
networkx>=3.2

# Data Validation and Settings
pydantic>=2.7
pydantic-settings>=2.2

# CLI and Logging
typer[all]>=0.12
loguru>=0.7
python-dotenv>=1.0

# Token Counting (context budget)
tiktoken>=0.7

# Evaluation
scipy>=1.13
pandas>=2.2
numpy>=1.26

# HF Hub (pinned above in Embedding section)
