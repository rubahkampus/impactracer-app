# AST Parsing (Offline Indexer)
tree-sitter>=0.22
tree-sitter-languages>=1.10.2
mistune>=3.0

# Embedding and Reranking (Local Inference)
# FlagEmbedding 1.3.5 is incompatible with transformers 5.x.
# Pin transformers to the latest 4.x release.
FlagEmbedding>=1.2.5
transformers>=4.44.2,<5.0

# LLM Client (OpenRouter via httpx — exclusive provider)
httpx>=0.27

# Vector and Relational Stores
chromadb>=0.5.0
rank_bm25>=0.2.2

# In-Memory Graph
networkx>=3.2

# Data Validation and Settings
pydantic>=2.7
pydantic-settings>=2.2

# CLI and Logging
typer[all]>=0.12
loguru>=0.7
python-dotenv>=1.0

# Token Counting (context budget)
tiktoken>=0.7

# Evaluation
scipy>=1.13
pandas>=2.2
numpy>=1.26

# HF Hub (first-run model cache)
huggingface-hub>=0.23
